// This source code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © darkgosh
//Create by Renato Vidana
//Last Update: 27 February, 2023
//@version=5
// Use the User Inputs
indicator("Eg. User Inputs class 10")

//Get a boolean input value
//inputBoolean = input.bool(title="On/Off", defval = true )
//inputBoolean = input.bool(defval = true ) // si no pones titulo Pine script elige uno tomando el nombre de la vareable
//inputBoolean = input.bool(title="On/Off", defval = true, tooltip = "Turn On / Off this setting")//se usa para explicar como se usa o que datos debes meter.


//Inline se utiliza para colocar en la misma linea colocando X y Y para colocarla en la linea siguiente
inputBoolean = input.bool(title="On/Off", defval = true, tooltip = "Turn On / Off this setting", inline = "X")
inputBoolean1 = input.bool(title="Other", defval = true, tooltip = "Turn On / Off this setting", inline = "y")
inputBoolean2 = input.bool(title="On/Off", defval = true, tooltip = "Turn On / Off this setting", inline = "X")
inputBoolean3 = input.bool(title="On/Off", defval = true, tooltip = "Turn On / Off this setting", 
                           group = "General Settings")
inputBoolean4 = input.bool(title="On/Off", defval = true, tooltip = "Turn On / Off this setting", confirm = true)


//Get a color input
inputColor= input.color(title = "Color", defval = color.black)
//Get inger Input value
InputInteger = input.int(title = 'Whole Number' ,defval = 5 , minval = -10, maxval = 10)
InputInteger2 = input.int(title = 'Whole Number' ,defval = 5 , options = [1,2,3,4,5])


//get a float input value
inputFloat = input.float(title="Decimal Number", defval =-0.5 , minval =-3.5 , maxval =3.14 , step = 0.01 )

//get Symbol input value
inputSymbol = input.symbol(title = "Symbol" ,defval = "SPY")

// Get a Price Input
inputPrice = input.price(title ="price" , defval = 0.0, confirm = true)

// Get a source value
inputSource = input.source(title = "Source",defval =close )

//---- GET THE TIME INPUTS ----
var G_TIME = "Time Settigs"

//Get a time & date input
inputTime = input.time(title ="Time" , defval =timestamp("05 feb 2023 13:30 +0000"), group=G_TIME )

//Get resolutuin input value
//inputResolution = input.timeframe(title = "TimeFrame", defval = "60", group ="Time Settings" )
inputResolution = input.timeframe(title = "TimeFrame", defval = "60", group =G_TIME )

//Get a string input
inputString = input.string(title = "String", defval = "A", options = ["A","B","C"], tooltip = "Select an Option")

//Get a Session input Value
inputSession = input.session(title = "Session", defval = "0300-1300", group = G_TIME)



//draw nothing to the chart (required for script to compile)
plot(na)
